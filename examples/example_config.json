{
  "_meta": {
    "purpose": "Example runtime config. Drop a file like this next to the CLI as config.json to change default behaviour.",
    "usage": "profiles (config/profiles.json) take precedence over these keys; CLI flags override both.",
    "copy": "cp examples/example_config.json config/config.json   (then edit)"
  },
  "threads": 40,
  "scan_concurrency": 800,
  "http_timeout": 10,
  "rotate_ua": true,
  "delay": 0.4,
  "crawl_max_pages": 80,
  "crawl_max_depth": 4,
  "dir_threads": 20,
  "max_injection_points": 80,
  "time_based_sqli": true,
  "form_attempts": 1200,
  "rate_limit_attempts": 10,
  "bucket_max_checks": 30,
  "crack_budget_pwds": 6000,
  "oob_enabled": true,
  "oob_port": 45678,
  "report_format": "all",
  "mitre_tagging": true
}